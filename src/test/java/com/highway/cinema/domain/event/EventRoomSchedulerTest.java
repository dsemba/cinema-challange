package com.highway.cinema.domain.event;

import org.junit.Ignore;
import org.junit.Test;

public class EventRoomSchedulerTest {

    @Ignore("Not implemented yet")
    @Test(timeout = 1000)
    public void testEfficiency() {
        //TODO: Schedule a lot of events lock per day and room
        //given
        //a number of events with random room (5) and random datetime within next 2 weeks and a movie

        //when
        //schedule events ignore errors caused by overlapping

        //then
        //finished in most efficient time
    }
}